#!/usr/bin/env node
'use strict';
require('../src/cli').main(process.argv).then(
  (code) => process.exit(code || 0),
  (err) => { console.error('[agentwatch] fatal:', err && err.stack || err); process.exit(1); }
);
